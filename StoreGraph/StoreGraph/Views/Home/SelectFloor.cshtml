@model StoreGraph.Models.StoreModel

@{
    ViewBag.Title = "SelectFloor";
}

@using (Html.BeginForm("RenderProfitsPerBay", "Graph"))
{
    @Html.HiddenFor(id => id.SelectedStoreId);
    @Html.HiddenFor(floor => floor.SelectedStoreFloor);
    <input type="submit" value="Get Profits Per Bay" />
}

@using (Html.BeginForm("RenderVolumePerBay", "Graph"))
{
    @Html.HiddenFor(id => id.SelectedStoreId);
    @Html.HiddenFor(floor => floor.SelectedStoreFloor);
    <input type="submit" value="Get volume per bay" />
}