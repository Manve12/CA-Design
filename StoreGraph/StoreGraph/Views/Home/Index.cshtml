@model StoreGraph.Models.StoreModel

@{
    ViewBag.Title = "Home Page";
}

@using (Html.BeginForm("SelectStore", "Home"))
{
    @Html.DropDownListFor(id => id.SelectedStoreID, new SelectList(ViewBag.StoreIDs), new { onchange = "" })

    <input type="submit" value="Select store" />
}
